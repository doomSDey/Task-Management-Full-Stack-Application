export enum TaskTabs {
    All = "All",
    ToDo = "To Do",
    InProgress = "In Progress",
    Done = "Done"
}

export  enum TaskStatus {
    ToDo = "To Do",
    InProgress = "In Progress",
    Done = "Done"
}

export enum ModalTypes {
    Filter
}